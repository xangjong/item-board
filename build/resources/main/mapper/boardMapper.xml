<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spring.board.mapper.BoardMapper">

    <!-- 비품 리스트 전체 조회-->
    <select id="listAll" resultType="com.spring.board.dto.BoardDTO">
        SELECT * FROM TB_BOARD ORDER BY ITEM_NO
    </select>

    <!-- 비품 리스트 상세 조회   -->


    <!-- 비품 등록 -->
    <insert id="insertItem" parameterType="com.spring.board.dto.BoardDTO">
        INSERT INTO TB_BOARD (ITEM_NO, ITEM_NAME, ITEM_DETAIL, ITEM_QUANTITY, ITEM_PRICE,
            ITEM_IMAGE, REG_DATETIME, MOD_DATETIME)
        VALUES (#{item_No}, #{item_Name}, #{item_Detail}, #{item_Quantity}, #{item_Price},
            #{item_Image}, #{reg_DateTIme}, #{mod_DateTime})
    </insert>

    <!-- 비품 정보 수정  -->

    <!--  비품 정보 삭제  -->



</mapper>