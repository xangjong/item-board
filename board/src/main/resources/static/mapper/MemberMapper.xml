<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.item.board.mapper.MemberMapper">

    <select id="getUserAccount" resultType="com.item.board.dto.Member">
        SELECT * FROM MEMBER
        WHERE MEMBER_ID= #{memberId}
    </select>

    <insert id="createUser" parameterType="com.item.board.dto.Member">
        INSERT INTO TB_MEMBER
            (MEMBER_ID, MEMBER_PW, MEMBER_NAME, MEMBER_NUMBER, MEMBER_OFFICE, MEMBER_ROLE)
        VALUES(#{memberId},#{memberPw},#{memberName},#{memberNumber}, #{memberOffice}, #{memberRole});
    </insert>
</mapper>